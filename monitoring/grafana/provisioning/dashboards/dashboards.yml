apiVersion: 1

providers:
  - name: default
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
      foldersFromFilesStructure: false

# Pre-built dashboards to import from grafana.com:
#   Proxmox VE:    ID 10347
#   UniFi:         ID 11315
#   Node Exporter: ID 1860
#
# Import via Grafana UI: Dashboards → Import → enter the ID
